# LAM_Audio2Expression_WebGL

**Lightweight WebGL Renderer** --- A real-time 3D Gaussian Splatting
rendering engine used in **LAM** and **LAM_Audio2Expression** pipelines
for interactive avatar animation.

The renderer is also available as an NPM package:

👉 https://www.npmjs.com/package/gaussian-splat-renderer-for-lam

------------------------------------------------------------------------

## 🚀 Overview

LAM_WebRender provides a real-time web-based renderer for avatars
generated by **LAM** and animated using ARKit blendshapes produced by
**LAM_Audio2Expression**.

Pipeline overview:

Audio → Expression Generation → Avatar Animation → Web Rendering

To run the web application, the backend Audio-to-Expression server must
be started before launching the frontend.

------------------------------------------------------------------------

## 📦 Prerequisites

Before running the web application, you must set up
**LAM_Audio2Expression**.

### Step 1 --- Install LAM_Audio2Expression

Enter the folder:

``` bash
cd LAM_Audio2Expression
```

Install CUDA dependencies:

``` bash
bash ./scripts/install/install_cu118.sh
```

Download pretrained models:

``` bash
huggingface-cli download 3DAIGC/LAM_audio2exp --local-dir ./
```

Extract assets:

``` bash
tar -xzvf LAM_audio2exp_assets.tar
tar -xzvf LAM_audio2exp_streaming.tar
```

------------------------------------------------------------------------

### Step 2 --- Return to Project Root

``` bash
cd ..
```

------------------------------------------------------------------------

### Step 3 --- Run Backend Server

Run the Audio-to-Expression API server:

``` bash
nohup python -m server.api &
```

The backend server will run at:

    http://localhost:8001

------------------------------------------------------------------------

## 🎤 Audio Avatar Web Application

The web application includes:

-   A 3D avatar
-   An audio upload button

Workflow:

1.  User uploads an audio file.
2.  Audio is sent to backend.
3.  LAM_Audio2Expression generates ARKit blendshapes.
4.  Avatar expressions are animated in real time.

------------------------------------------------------------------------

## 🖥 Running the Web App (Frontend)

Install dependencies:

``` bash
npm install
```

Run development server:

``` bash
npm run dev
```

Open in browser:

    http://localhost:5173/

------------------------------------------------------------------------

## ⚙ Backend Configuration

If the backend runs on another host or port, configure `.env`:

``` env
VITE_AUDIO2EXPRESSION_API=http://localhost:8001
```

------------------------------------------------------------------------

## 📦 Core Features

-   Real-time avatar rendering
-   ARKit blendshape animation
-   Gaussian splatting renderer
-   NPM package integration
-   Low-latency interaction

------------------------------------------------------------------------

## 📦 NPM Package Usage

### Installation

``` bash
npm install gaussian-splat-renderer-for-lam
```

### Basic Usage

``` javascript
import * as GaussianSplats3D from 'gaussian-splat-renderer-for-lam';

const div = document.getElementById('GaussianRenderer');
const assetPath = './asset/arkit/p2-1.zip';

const render = await GaussianSplats3D.GaussianSplatRenderer
    .getInstance(div, assetPath);
```

### Advanced Usage

-   Load expression JSON files to animate avatars
-   Replace avatar assets with models generated using LAM

------------------------------------------------------------------------

## 🧪 Example Files

    asset/arkit/p2-1.zip              -> Avatar model
    asset/test_expression_1s.json     -> Expression data

------------------------------------------------------------------------

## 🔗 Related Projects

  -----------------------------------------------------------------------------------------------
  Feature                           Repository
  --------------------------------- -------------------------------------------------------------
  Chat integration                  https://github.com/HumanAIGC-Engineering/OpenAvatarChat.git

  Avatar generation                 https://github.com/aigc3d/LAM.git

  Expression generation             https://github.com/aigc3d/LAM_Audio2Expression.git
  -----------------------------------------------------------------------------------------------

------------------------------------------------------------------------

## 📺 Demo

LAM_Audio2Expression converts audio input into ARKit blendshapes, which
animate avatars generated by LAM and rendered with this engine.

::: {align="center"}
`<video controls src="https://github.com/user-attachments/assets/2bb4e74f-cd96-4c50-9833-fae10b1ead4c">`{=html}`</video>`{=html}
:::

------------------------------------------------------------------------

Low-latency SDK demo for interactive chat avatars:

::: {align="center"}
`<video controls src="https://github.com/user-attachments/assets/98f66655-e1c1-40a9-ab58-bdd49dafedda" width="80%">`{=html}`</video>`{=html}
:::

------------------------------------------------------------------------

## 📜 License

This project is licensed under the MIT License.
