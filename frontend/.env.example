# Leave empty in dev — Vite proxies /api to the backend.
# Must match BACKEND_PORT in backend/.env (default 8001).
VITE_BACKEND_PORT=8001

# Or set full backend URL (overrides VITE_BACKEND_PORT):
# VITE_AUDIO2EXPRESSION_API=http://your-server:8001
